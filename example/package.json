{
  "name": "@ethersproject/examples",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist coverage",
    "format": "prettier --write tests/{*,**/*}.ts",
    "lint": "yarn prettier --list-different tests/{*,**/*}.ts",
    "test": "jest",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@crestproject/ethers-contracts": "^0.0.0",
    "@nomiclabs/buidler": "^1.3.8"
  }
}
